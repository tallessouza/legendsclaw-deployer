---
epic: 9
story: 9.1
title: "install.sh — One-Liner curl|bash para VM limpa"
status: Draft
executor: "@dev"
quality_gate: "@devops"
quality_gate_tools: [script_review, idempotency_test]
---

# 9.1 — install.sh: One-Liner curl|bash para VM limpa

## Description

Criar um script `install.sh` na raiz do repo deployer que serve como ponto de entrada único para provisionar uma VM limpa. O usuário executa:

```bash
bash <(curl -sSL https://raw.githubusercontent.com/<org>/legendsclaw-deployer/main/install.sh)
```

O script:
1. Verifica pré-requisitos (root, OS, conectividade)
2. Instala git se necessário
3. Clona o repo deployer para `/opt/legendsclaw`
4. Executa `setup.sh` (instala deps: docker, node, jq, etc.)
5. Exibe instruções para rodar `deployer.sh`

## Context

O deployer será extraído para um repo próprio (`legendsclaw-deployer`) contendo apenas o necessário para deploy. A estrutura no novo repo será flat (sem prefixo `deployer/`):

```
legendsclaw-deployer/
├── install.sh          ← NOVO (este story)
├── setup.sh
├── deployer.sh
├── deployer-auto.sh
├── auto-config.example
├── lib/
├── ferramentas/
├── apps/
├── migrations/
└── seeds/
```

## Acceptance Criteria

- [ ] AC1: `install.sh` existe na raiz do repo
- [ ] AC2: Funciona via `bash <(curl -sSL <raw-url>)` numa Ubuntu 22.04 limpa
- [ ] AC3: Verifica root, OS (soft gate), e conectividade antes de prosseguir
- [ ] AC4: Instala `git` via apt se não estiver presente
- [ ] AC5: Clona o repo para `/opt/legendsclaw` (skip se já existe, `git pull` para atualizar)
- [ ] AC6: Executa `setup.sh` automaticamente após clone
- [ ] AC7: Se `setup.sh` falhar, exibe erro claro e para
- [ ] AC8: No final, exibe instruções claras: `cd /opt/legendsclaw && bash deployer.sh`
- [ ] AC9: Idempotente — rodar duas vezes não quebra nada
- [ ] AC10: Segue o pattern visual do setup.sh (feedback N/M, cores ANSI)
- [ ] AC11: Log salvo em `~/legendsclaw-logs/install-<timestamp>.log`

## Scope

### IN
- Script `install.sh` na raiz do repo
- git clone + git pull logic
- Instalação de git se ausente
- Chamada do `setup.sh` existente
- Feedback visual e logging

### OUT
- Auto-config (manual — usuário roda deployer.sh interativo)
- Modificações no `setup.sh` existente
- Separação do repo (story 9.2)

## Technical Notes

- O `setup.sh` já instala: docker, node 22, pnpm, jq, git, python3, htpasswd
- O `setup.sh` já cria `~/dados_vps/` e `~/legendsclaw-logs/`
- O `install.sh` deve ser **fino** (~60-80 linhas) — a lógica pesada fica no `setup.sh`
- Usar mesmo pattern visual: `N/M - [ OK ] - mensagem`
- TOTAL_STEPS: ~8 (root, os, conectividade, git, clone, setup, instruções, log)

## Dependencies

- Repo deployer acessível via HTTPS (público)

## File List

- [ ] `install.sh` (novo — raiz do repo)

## Complexity

- **Estimate:** P (pequeno)
- **Risk:** LOW — wrapper fino sobre infraestrutura existente

## Change Log

| Data | Agente | Mudança |
|------|--------|---------|
| 2026-02-21 | @pm (Morgan) | Story criada |
