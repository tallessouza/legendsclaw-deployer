---
epic: 12
story: 12.1
title: "Configurar OpenClaw Local em Mode Remote"
status: Ready
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: [code_review, pattern_validation, integration_test]
complexity: M
risk: HIGH
---

# 12.1 — Configurar OpenClaw Local em Mode Remote

## Executor Assignment

```yaml
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: [code_review, pattern_validation, integration_test]
```

## Story

**As a** usuário que quer usar o agente conectado ao gateway remoto,
**I want** um script que instale o OpenClaw e configure `~/.openclaw/openclaw.json` com mode:remote na minha máquina local,
**so that** eu consiga conectar ao gateway na VPS via WSS e ter acesso ao agente com skills e workspace.

## Description

Criar `deployer/ferramentas/setup-local-openclaw.sh` que:
1. Instala OpenClaw na máquina local (se não instalado)
2. **Gera `~/.openclaw/openclaw.json` localmente** usando dados do `dados_bridge` (hostname, tailnet, gateway password) — auto-suficiente, sem depender de arquivo da VPS
3. Testa conexão WSS ao gateway VPS

**IMPORTANTE:** O config vai em `~/.openclaw/openclaw.json` (path real do OpenClaw), **NÃO** em `~/.aiosbot/`. O OpenClaw procura config em:
1. `~/.openclaw/openclaw.json` (primário)
2. `~/.openclaw/clawdbot.json` (fallback)
3. Override via `OPENCLAW_HOME` env var

**Integração no install.sh:**
```
Step 6: setup-local.sh (deps)
Step 7: setup-local-bridge.sh (bridge)
Step 8: setup-local-openclaw.sh (OpenClaw remote) ← NOVO
Step 9: setup-local-aios.sh (AIOS + agent registration)
Step 10: setup-local-finish.sh (instruções)
```

## Context

### Sistema Existente

- **install.sh:** One-liner com modo `--local`
- **setup-local.sh:** Instala git, Node.js 22+, Claude Code CLI, Tailscale
- **setup-local-bridge.sh:** Configura Tailscale bridge, hooks Claude Code, service index
- **dados_bridge:** State file com GATEWAY_URL, Tailscale Hostname, Tailnet

### Referência OpenClaw (auditada)

- Config path: `~/.openclaw/openclaw.json`
- Env override: `OPENCLAW_HOME`, `OPENCLAW_STATE_DIR`
- Gateway mode:remote: `gateway.mode: "remote"`, `gateway.remote.url: "wss://..."`, `gateway.remote.password: "..."`

## Acceptance Criteria

- [ ] AC1: Script verifica Node.js >= 18
- [ ] AC2: Lê dados_bridge para Tailscale Hostname, Tailnet, gateway password
- [ ] AC3: Instala OpenClaw CLI (se não presente — `npm install -g openclaw` ou clone do repo)
- [ ] AC4: Cria `~/.openclaw/` se não existe
- [ ] AC5: Gera `~/.openclaw/openclaw.json` localmente usando dados do `dados_bridge` (hostname, tailnet, password) — sem depender de arquivo externo
- [ ] AC6: `~/.openclaw/openclaw.json` contém `gateway.mode: "remote"` + `gateway.remote.url: "wss://{hostname}.{tailnet}.ts.net"` + `gateway.remote.password`
- [ ] AC7: Testa conexão WSS ao gateway VPS (graceful fallback se offline)
- [ ] AC8: Salva estado em `~/dados_vps/dados_local_openclaw` (local usa mesmo state dir `~/dados_vps/` para consistência com bridge/aios)
- [ ] AC9: Idempotente (SKIP se já configurado e versão ok)
- [ ] AC10: install.sh atualizado com step setup-local-openclaw.sh entre bridge e aios

## Scope

### IN
- Nova ferramenta `deployer/ferramentas/setup-local-openclaw.sh`
- Config em `~/.openclaw/openclaw.json` com mode:remote
- Teste de conexão WSS
- State file dados_local_openclaw
- Atualização do install.sh

### OUT
- Não instala Node.js (setup-local.sh faz isso)
- Não configura Tailscale (setup-local.sh faz isso)
- Não gera workspace files (story 12.4)
- Não configura skills (story 12.5)
- **NÃO cria `~/.aiosbot/`** — path obsoleto

## Risks

| Risk | Impact | Mitigation |
|------|--------|------------|
| OpenClaw CLI não disponível via npm | High | Fallback para clone de `https://github.com/openclaw-ai/openclaw` + `pnpm install` |
| WSS endpoint variável entre versões | Medium | URL completa do tailnet (padrão OpenClaw) |
| dados_bridge inexistente | Medium | Coleta interativa como fallback |

## Tasks / Subtasks

- [ ] **Task 1: Criar `deployer/ferramentas/setup-local-openclaw.sh`**
  - [ ] 1.1 Source libs (ui.sh, logger.sh, common.sh, auto.sh, hints.sh, env-detect.sh)
  - [ ] 1.2 Verificar Node.js >= 18
  - [ ] 1.3 Ler dados_bridge (Tailscale Hostname, Tailnet, gateway password)
  - [ ] 1.4 Ler dados_whitelabel (nome_agente)
  - [ ] 1.5 Instalar/verificar OpenClaw CLI
  - [ ] 1.6 Criar `~/.openclaw/` se não existe
  - [ ] 1.7 Gerar `~/.openclaw/openclaw.json` com mode:remote usando dados do dados_bridge (hostname, tailnet, password)
  - [ ] 1.8 Testar conexão WSS (graceful fallback)
  - [ ] 1.9 Salvar estado em dados_local_openclaw
  - [ ] 1.10 Idempotência (skip se configurado)
  - [ ] 1.11 Hints de próximos passos

- [ ] **Task 2: Atualizar `deployer/install.sh`**
  - [ ] 2.1 Adicionar step setup-local-openclaw.sh entre bridge e aios
  - [ ] 2.2 Incrementar TOTAL_STEPS
  - [ ] 2.3 Manter pause_between_steps

- [ ] **Task 3: Testes**
  - [ ] 3.1 Testar criação de ~/.openclaw/ structure
  - [ ] 3.2 Testar que ~/.openclaw/openclaw.json tem gateway.mode: "remote"
  - [ ] 3.3 Testar que ~/.openclaw/openclaw.json tem gateway.remote.url com WSS
  - [ ] 3.4 Testar idempotência (skip se existe)
  - [ ] 3.5 Testar install.sh tem o step correto

## Dev Notes

### openclaw.json mode:remote (formato real OpenClaw)

```json
{
  "gateway": {
    "mode": "remote",
    "remote": {
      "url": "wss://hostname.tailnet.ts.net",
      "password": "gateway_password"
    }
  },
  "agents": {
    "defaults": {
      "model": { "primary": "openrouter/auto" },
      "workspace": "~/.openclaw/workspace"
    }
  }
}
```

**Nota:** O workspace local pode apontar para `~/.openclaw/workspace` (default) ou para um path customizado. No mode:remote, a maioria das operações são executadas no gateway VPS, então o workspace local é menos crítico.

### State File Format

```
OpenClaw Version: X.Y.Z
Config Path: ~/.openclaw/openclaw.json
Gateway Mode: remote
Gateway URL: wss://hostname.tailnet.ts.net
WSS Test: OK|FAIL|SKIP
Data Configuracao: YYYY-MM-DD HH:MM:SS
```

### Source Tree Relevante

```
deployer/
├── ferramentas/
│   ├── setup-local.sh              # Step 6: deps (Node.js, Tailscale)
│   ├── setup-local-bridge.sh       # Step 7: bridge Tailscale
│   ├── setup-local-openclaw.sh     # Step 8: OpenClaw remote ← NOVO
│   ├── setup-local-aios.sh         # Step 9: AIOS + agent registration
│   └── setup-local-finish.sh       # Step 10: instruções finais
├── lib/
│   ├── ui.sh
│   ├── logger.sh
│   ├── common.sh
│   ├── auto.sh
│   ├── hints.sh
│   └── env-detect.sh
├── install.sh                      # One-liner (--local mode)
└── apps/{agent}/config/
    └── openclaw-local.json          # Gerado por 12.2 (referência VPS, não usado por 12.1)
tests/deployer/
└── setup-local-openclaw.bats       # NOVO
```

### Padrões a Seguir

```bash
#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
LIB_DIR="${SCRIPT_DIR}/../lib"
source "${LIB_DIR}/ui.sh"
source "${LIB_DIR}/logger.sh"
# ...
```

### Testing

- **Framework:** BATS (Bash Automated Testing System) — `npx bats tests/deployer/setup-local-openclaw.bats`
- **Localização:** `tests/deployer/setup-local-openclaw.bats`
- **Padrão de source:** `source <(sed 's/^readonly //g' "$LIB_DIR/file.sh")` para permitir override de readonly vars em testes
- **Teardown:** sempre `rm -rf "$TEST_DIR" 2>/dev/null || true`
- **Referência:** Ver testes existentes em `tests/deployer/` para padrões de mock e setup

## CodeRabbit Integration

> **CodeRabbit Integration**: Disabled
>
> CodeRabbit CLI is not enabled in `core-config.yaml`.
> Quality validation will use manual review process only.
> To enable, set `coderabbit_integration.enabled: true` in core-config.yaml

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-02-25 | 1.0 | Story criada a partir do Epic 12 | @pm (Morgan) |
| 2026-02-25 | 2.0 | v3 — corrigido para usar ~/.openclaw/openclaw.json (não ~/.aiosbot/), auditado contra OpenClaw real | @pm (Morgan) |
| 2026-02-25 | 2.1 | Validação PO: adicionado CodeRabbit skip notice, seção Testing (BATS), source tree, clarificação state dir, URL repo OpenClaw | @po (Pax) |
| 2026-02-25 | 2.2 | Fix CRITICAL: script gera JSON localmente via dados_bridge (auto-suficiente), elimina dependência de 12.2 e mecanismo de transferência indefinido | @po (Pax) |
