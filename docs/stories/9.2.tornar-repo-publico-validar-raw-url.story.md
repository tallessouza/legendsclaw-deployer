---
epic: 9
story: 9.2
title: "Extrair deployer para repo standalone"
status: Draft
executor: "@devops"
quality_gate: "@dev"
quality_gate_tools: [structure_validation, path_test, secret_scan]
---

# 9.2 — Extrair deployer para repo standalone

## Description

Criar o repositório `legendsclaw-deployer` (público) contendo apenas os arquivos necessários para o deploy funcionar de forma independente. O deployer já é praticamente standalone — as únicas referências externas têm fallback.

## Current State (no legendsclaw)

```
legendsclaw/
├── deployer/
│   ├── setup.sh
│   ├── deployer.sh
│   ├── deployer-auto.sh
│   ├── auto-config.example
│   ├── lib/          (8 arquivos)
│   ├── ferramentas/  (15 scripts)
│   ├── apps/         (agent bundles)
│   ├── migrations/
│   └── seeds/
└── .aios-core/infrastructure/services/bridge.js  ← ref externa (opcional)
```

## Target State (repo novo)

```
legendsclaw-deployer/     ← repo público
├── install.sh            (story 9.1)
├── setup.sh
├── deployer.sh
├── deployer-auto.sh
├── auto-config.example
├── lib/
├── ferramentas/
├── apps/
├── migrations/
├── seeds/
├── .gitignore
└── README.md
```

## Acceptance Criteria

- [ ] AC1: Repo `legendsclaw-deployer` criado no GitHub (público)
- [ ] AC2: Scan de secrets antes de publicar (nenhum .env, key, password commitado)
- [ ] AC3: Estrutura flat — sem prefixo `deployer/` nos caminhos
- [ ] AC4: Todos os paths internos ajustados (SCRIPT_DIR, LIB_DIR, etc.)
- [ ] AC5: `deployer.sh` funciona da raiz do novo repo
- [ ] AC6: `setup.sh` funciona da raiz do novo repo
- [ ] AC7: Referências a `.aios-core/` removidas ou tornadas opcionais com fallback claro
- [ ] AC8: `.gitignore` cobre: `.env`, `dados_vps/`, `*.log`, `node_modules/`
- [ ] AC9: `curl | bash install.sh` funciona apontando para o novo repo
- [ ] AC10: README.md com instruções de uso (one-liner de instalação)

## Scope

### IN
- Criação do repo `legendsclaw-deployer`
- Cópia dos arquivos do deployer
- Ajuste de paths relativos
- Scan de secrets
- README básico
- .gitignore

### OUT
- Manter sync entre repos (decisão futura)
- CI/CD no novo repo
- Remover deployer/ do legendsclaw original

## Technical Notes

### Fato chave: deployer gera tudo inline

O deployer **não copia nada** do `.aios-core/` nem do `aiosbot-vps-stack-master`. Tudo é gerado via heredoc:
- `05-whitelabel` → gera `apps/{agente}/config/`, `hooks/`, `lib/`, `skills/`
- `06-workspace` → gera SOUL.md, IDENTITY.md, USER.md, AGENTS.md, etc.
- `07-llm-router` → gera `llm-router-config.yaml`
- `08-skills` → gera `skills/{name}/index.js`

O `deployer/apps/mano/` é um snapshot de execução anterior — serve como referência.

### Referências externas a ajustar (3 pontos)

1. **12-bridge.sh** — `SERVICES_DIR=".aios-core/infrastructure/services"`
   - Ação: mover `bridge.js` para `lib/bridge.js` ou gerar inline

2. **15-validacao-final.sh** — `bridge_file=".aios-core/infrastructure/services/bridge.js"`
   - Ação: apontar para novo path ou skip (já tem fallback)

3. **lib/hints.sh** — mensagens com paths `.aios-core/...`
   - Ação: atualizar strings de hint

### Destinos na VPS (runtime, não deps)

- `~/.openclaw/workspace/` — 06 e 08 copiam pra lá (skip graceful se não existe)
- `/opt/openclaw/.env` — 08 popula com API keys (skip graceful)

### Paths internos (já funcionam flat)

```bash
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
LIB_DIR="${SCRIPT_DIR}/../lib"
```

## Dependencies

- Story 9.1 (install.sh pronto)

## File List

- [ ] Novo repo `legendsclaw-deployer` com todos os arquivos
- [ ] `.gitignore` (novo)
- [ ] `README.md` (novo)
- [ ] `ferramentas/12-bridge.sh` (ajustar ref .aios-core)
- [ ] `ferramentas/15-validacao-final.sh` (ajustar ref .aios-core)

## Complexity

- **Estimate:** M (médio)
- **Risk:** MEDIUM — scan de secrets é crítico antes de tornar público

## Change Log

| Data | Agente | Mudança |
|------|--------|---------|
| 2026-02-21 | @pm (Morgan) | Story criada |
