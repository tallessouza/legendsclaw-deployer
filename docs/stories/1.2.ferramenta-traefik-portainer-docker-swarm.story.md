# Story 1.2: Ferramenta Traefik + Portainer + Docker Swarm

## Status

**InReview**

## Executor Assignment

```yaml
executor: "@devops"
quality_gate: "@architect"
quality_gate_tools: [infrastructure_review, config_validation, script_safety_check, network_topology_review]
```

## Story

**As a** operador,
**I want** instalar a base (Swarm + Traefik + Portainer) via deployer interativo,
**so that** tenha a fundacao de infraestrutura funcionando com HTTPS automatico.

## Acceptance Criteria

1. Gate de recursos: verifica 1 vCPU, 1GB RAM minimo
2. Coleta de inputs com loop confirmado: dominio Portainer, email SSL, user/senha Portainer, nome servidor, nome rede overlay
3. Hints de firewall com tabela de portas (22/TCP SSH, 80/TCP HTTP, 443/TCP HTTPS, 9443/TCP Portainer, 2377/TCP Swarm, 7946/TCP+UDP Swarm nodes, 4789/UDP overlay, 41641/UDP Tailscale)
4. Hints de DNS com tabela de registros tipo A
5. Inicializa Docker Swarm (retry 3x)
6. Cria overlay network
7. Gera `~/traefik.yaml` (Traefik v3.5.3 + Let's Encrypt)
8. Gera `~/portainer.yaml` (Agent + CE + Traefik labels)
9. Deploy via `docker stack deploy` (primeiro deploy, Portainer ainda nao existe)
10. Wait com polling: Traefik → Portainer
11. Cria conta admin no Portainer via API (retry 4x)
12. Salva credenciais em `~/dados_vps/dados_portainer`
13. Logs salvos em `~/legendsclaw-logs/base-{timestamp}.log`

## CodeRabbit Integration

> **CodeRabbit Integration**: Disabled
>
> CodeRabbit CLI is not enabled in `core-config.yaml`.
> Quality validation will use manual review process only.
> To enable, set `coderabbit_integration.enabled: true` in core-config.yaml

## Tasks / Subtasks

- [x] Task 1: Modularizar funcoes utilitarias do setup.sh para deployer/lib/ (AC: pre-requisito)
  - [x] 1.1: Criar `deployer/lib/ui.sh` — extrair funcoes `step_ok()`, `step_fail()`, `step_skip()`, `feedback()` do setup.sh
  - [x] 1.2: Criar `deployer/lib/logger.sh` — extrair funcoes `log_init()`, `log()`, `log_finish()` com pattern `{ferramenta}-{timestamp}.log`
  - [x] 1.3: Criar `deployer/lib/common.sh` — implementar `dados()`, `recursos()`, `verificar_stack()`, `validar_senha()`
  - [x] 1.4: Criar `deployer/lib/deploy.sh` — implementar `stack_editavel()` (deploy via Portainer API), `wait_stack()` (polling), `pull()`
  - [x] 1.5: Criar `deployer/lib/hints.sh` — implementar `hint_dns()`, `hint_firewall()`, `hint_provider()`
  - [x] 1.6: Verificar que setup.sh continua funcional apos extracao (nao quebrar Story 1.1)
  - **NOTA:** setup.sh MANTÉM funcoes inline (standalone para curl|bash). lib/ contem implementacoes independentes para uso pelas ferramentas. NAO criar dependencia entre setup.sh e lib/.

- [x] Task 2: Implementar gate de recursos (AC: 1)
  - [x] 2.1: Funcao `recursos()` em common.sh — verifica vCPU e RAM disponivel
  - [x] 2.2: Minimos para 01-base: 1 vCPU, 1GB RAM
  - [x] 2.3: Se nao atende, perguntar se deseja continuar (soft gate)

- [x] Task 3: Implementar coleta de inputs com loop confirmado (AC: 2)
  - [x] 3.1: Coletar: dominio Portainer, email SSL, usuario Portainer, senha Portainer, nome servidor, nome rede overlay
  - [x] 3.2: Funcao `validar_senha()` — minimo 8 chars, pelo menos 1 maiuscula, 1 numero
  - [x] 3.3: Exibir resumo `conferindo_as_info` com todos os dados coletados
  - [x] 3.4: Loop `while true`: se usuario confirma → prosseguir; se nao → recoletar

- [x] Task 4: Implementar hints contextuais (AC: 3, 4)
  - [x] 4.1: `hint_firewall()` — tabela de portas: 22/TCP SSH, 80/TCP HTTP, 443/TCP HTTPS, 9443/TCP Portainer, 2377/TCP Swarm, 7946/TCP+UDP Swarm nodes, 4789/UDP overlay, 41641/UDP Tailscale
  - [x] 4.2: `hint_dns()` — tabela de registros A necessarios (portainer.dominio.com → IP da VPS)
  - [x] 4.3: Exibir hints ANTES da coleta de inputs (usuario precisa configurar antes)

- [x] Task 5: Inicializar Docker Swarm (AC: 5, 6)
  - [x] 5.1: Detectar IP principal da VPS para `--advertise-addr`
  - [x] 5.2: `docker swarm init --advertise-addr $ip` com retry 3x (sleep 5s entre tentativas)
  - [x] 5.3: Criar overlay network: `docker network create --driver=overlay $nome_rede`
  - [x] 5.4: Se Swarm ja inicializado, detectar e pular com `[ SKIP ]`
  - [x] 5.5: Se overlay network ja existe, pular com `[ SKIP ]`

- [x] Task 6: Gerar YAMLs de stack (AC: 7, 8)
  - [x] 6.1: YAML gerado via heredoc inline em 01-base.sh (Orion pattern — decisao Pre-Flight)
  - [x] 6.2: Traefik v3.5.3 + Let's Encrypt + entrypoints 80/443 + overlay network
  - [x] 6.3: Portainer Agent + CE + Traefik labels para HTTPS + overlay network
  - [x] 6.4: YAMLs usam `external: true` em networks, `deploy.constraints: node.role == manager`, resource limits

- [x] Task 7: Deploy das stacks (AC: 9, 10)
  - [x] 7.1: `docker stack deploy -c ~/traefik.yaml traefik` (primeiro deploy, sem Portainer API)
  - [x] 7.2: `wait_stack "traefik_traefik"` — polling ate Traefik ficar 1/1
  - [x] 7.3: `docker stack deploy -c ~/portainer.yaml portainer`
  - [x] 7.4: `wait_stack "portainer_portainer" "portainer_portainer_agent"` — polling ate ambos 1/1
  - [x] 7.5: Timeout de 5 minutos no polling (10 iteracoes x 30s)

- [x] Task 8: Criar conta admin do Portainer via API (AC: 11, 12)
  - [x] 8.1: POST `https://$dominio_portainer/api/users/admin/init` com retry 4x (sleep 15s)
  - [x] 8.2: POST `https://$dominio_portainer/api/auth` para obter JWT token
  - [x] 8.3: Salvar credenciais em `~/dados_vps/dados_portainer` (formato plaintext: Dominio, Usuario, Senha, Token)
  - [x] 8.4: Atualizar `~/dados_vps/dados_vps` com nome_servidor e rede_interna
  - [x] 8.5: `chmod 600 ~/dados_vps/dados_portainer` — proteger credenciais com permissoes restritas

- [x] Task 9: Implementar logging e resumo final (AC: 13)
  - [x] 9.1: `log_init` no inicio com header (data, hostname, OS)
  - [x] 9.2: Todos os comandos logados via `tee` em `~/legendsclaw-logs/base-{timestamp}.log`
  - [x] 9.3: Resumo final: status de cada step (OK/FAIL/SKIP), URLs de acesso, credenciais

- [x] Task 10: Criar deployer.sh com menu (pre-requisito futuro)
  - [x] 10.1: Criar `deployer/deployer.sh` — entry point principal com menu
  - [x] 10.2: Opcao `[01] Traefik + Portainer (base)` → chama `ferramentas/01-base.sh`
  - [x] 10.3: Opcoes futuras listadas como `[EM BREVE]`

- [x] Task 11: Testes com bats-core
  - [x] 11.1: Criar `tests/deployer/lib-common.bats` — testes para `dados()`, `recursos()`, `verificar_stack()`, `validar_senha()`
  - [x] 11.2: Criar `tests/deployer/lib-ui.bats` — testes para `step_ok()`, `step_fail()`, `step_skip()`
  - [x] 11.3: Criar `tests/deployer/lib-hints.bats` — testes para `hint_firewall()`, `hint_dns()`
  - [x] 11.4: Criar `tests/deployer/lib-deploy.bats` — testes para `wait_stack()`, `stack_editavel()` (mock Portainer API)
  - [x] 11.5: Criar `tests/deployer/01-base.bats` — testes de integracao para fluxo principal
  - [x] 11.6: Garantir que testes da Story 1.1 (`tests/deployer/setup.bats`) continuam passando

## Dev Notes

### Insights da Story 1.1

- Funcoes utilitarias (feedback, logging, install_if_missing) foram implementadas **inline no setup.sh** por decisao PO
- **Story 1.2 deve modularizar** essas funcoes para `deployer/lib/` antes de implementar 01-base.sh (Task 1)
- Testes usam `sed 's/^readonly //g'` para contornar variaveis readonly ao sourcear funcoes — manter este pattern
- Node.js install via NodeSource setup_22.x; pnpm via corepack enable
- 15 testes bats-core passando em `tests/deployer/setup.bats`

[Source: docs/stories/1.1.bootstrap-deployer-preparacao-ambiente.story.md#dev-agent-record]

### Arquitetura de Infraestrutura

**Topologia de rede:**
```
Internet → VPS Hetzner (Helsinki)
  └── Docker Swarm (manager, single-node)
       ├── Traefik (:80, :443) — reverse proxy + HTTPS automatico
       │    └── portainer.dominio.com → :9443
       └── Overlay Network: legendsclaw_net (todas as stacks conectadas)
```

**Stacks Docker Swarm:**
| Stack | Services | YAML Location |
|-------|----------|---------------|
| traefik | traefik | `~/traefik.yaml` |
| portainer | portainer_agent, portainer | `~/portainer.yaml` |

**ADR-001:** Docker Swarm over Docker Compose — overlay network, Portainer API, idempotent deploys
**ADR-002:** Portainer API over direct CLI — visual editing post-deploy, centralized management

[Source: docs/architecture/legendsclaw-architecture.md#5-arquitetura-de-infraestrutura]

### Deployer Structure (Target After 1.2)

```
deployer/
├── setup.sh                     # Bootstrap (curl | bash) — Story 1.1 ✓
├── deployer.sh                  # Entry point + menu — Story 1.2
├── lib/
│   ├── common.sh                # dados(), recursos(), verificar_stack(), validar_senha()
│   ├── deploy.sh                # stack_editavel(), wait_stack(), pull()
│   ├── hints.sh                 # hint_dns(), hint_firewall()
│   ├── ui.sh                    # step_ok(), step_fail(), step_skip()
│   └── logger.sh                # log_init(), log(), log_finish()
├── ferramentas/
│   └── 01-base.sh               # Traefik + Portainer + Docker Swarm
└── templates/
    ├── traefik.yaml.tmpl        # Traefik v3.5.3 stack template
    └── portainer.yaml.tmpl      # Portainer CE stack template
```

[Source: docs/architecture/legendsclaw-architecture.md#8-estrutura-do-projeto]

### 8-Step Tool Lifecycle (Ferramenta Pattern)

Cada `ferramentas/NN-nome.sh` segue este ciclo canonico:

1. **RESOURCE GATE** — `recursos 1 1` (vCPU, RAM)
2. **LOAD STATE** — `dados()` → le ~/dados_vps/
3. **INPUT COLLECTION** — loop confirmado (read → conferindo_as_info → Y/N)
4. **RESOLVE DEPENDENCIES** — verificar_docker, verificar_stack
5. **GENERATE YAML** — heredoc interpolado → ~/app.yaml
6. **DEPLOY** — `docker stack deploy` (Story 1.2 usa CLI direto, nao Portainer API, pois Portainer ainda nao existe)
7. **VERIFY** — wait_stack com polling 30s
8. **FINALIZE** — salvar credenciais + exibir resumo

[Source: docs/architecture/legendsclaw-architecture.md#6-deployer]

### Deploy Pattern Especifico para Story 1.2

**IMPORTANTE:** Esta e a UNICA ferramenta que faz deploy via `docker stack deploy` direto (CLI). Todas as ferramentas subsequentes usarao `stack_editavel()` via Portainer API. Motivo: Portainer ainda nao existe quando 01-base.sh executa.

**Portainer Admin API:**
```bash
# Criar admin (retry 4x, sleep 15s)
POST https://$dominio_portainer/api/users/admin/init
Body: {"Username": "$user", "Password": "$pass"}

# Obter JWT token
POST https://$dominio_portainer/api/auth
Body: {"username": "$user", "password": "$pass"}
Response: {"jwt": "eyJ..."}
```

[Source: docs/referencia/orion-script-racional.md#section-10]

### Portainer API para Deploys Futuros (stack_editavel)

```bash
# Autenticacao (retry 6x, sleep 5s)
TOKEN=$(curl -k -s -X POST -H "Content-Type: application/json" \
  -d '{"username":"$USER","password":"$PASS"}' \
  https://$PORTAINER_URL/api/auth | jq -r .jwt)

# Obter endpoint
ENDPOINT_ID=$(curl -k -s -X GET -H "Authorization: Bearer $TOKEN" \
  https://$PORTAINER_URL/api/endpoints | jq -r '.[0].Id')

# Criar stack
curl -k -s -X POST -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"name":"$STACK","stackFileContent":"...","swarmID":"$SWARM_ID","env":[]}' \
  "https://$PORTAINER_URL/api/stacks?type=1&method=string&endpointId=$ENDPOINT_ID"
```

[Source: docs/referencia/orion-script-racional.md#stack_editavel]

### wait_stack() — Polling Pattern

```bash
# Polling: docker service ls --filter "name=$service" | grep "1/1"
# Sleep 30s entre checks
# Timeout: 5 minutos (10 iteracoes)
# Usar associative array para track de multiplos services
```

[Source: docs/referencia/orion-script-racional.md#wait_stack]

### Firewall Ports Reference

| Porta | Protocolo | Servico |
|-------|-----------|---------|
| 22 | TCP | SSH |
| 80 | TCP | HTTP (Traefik) |
| 443 | TCP | HTTPS (Traefik) |
| 9443 | TCP | Portainer |
| 2377 | TCP | Docker Swarm manager |
| 7946 | TCP+UDP | Swarm node communication |
| 4789 | UDP | Overlay network |
| 41641 | UDP | Tailscale |

[Source: docs/prd/epic-1-base-infrastructure-traefik-portainer-docker-swarm.md#story-1.2]

### Estado no Filesystem

**dados_vps (atualizar):**
```
Nome do Servidor: {nome_servidor}
Rede interna: {nome_rede}
```

**dados_portainer (criar):**
```
Dominio do portainer: {dominio_portainer}
Usuario: {user_portainer}
Senha: {pass_portainer}
Token: {jwt_token}
```

[Source: docs/architecture/legendsclaw-architecture.md#5-arquitetura-de-infraestrutura]

### YAML Stack Patterns

- `version: "3.7"` para Docker Swarm
- `external: true` em networks — reutiliza overlay ja criada
- `deploy.constraints: [node.role == manager]` — single-node
- `deploy.resources.limits` — CPU e memoria por service
- Traefik labels em cada service para routing automatico
- Let's Encrypt via `certificatesresolvers.letsencryptresolver.acme`

[Source: docs/architecture/legendsclaw-architecture.md#9-stacks-docker-swarm]

### Coding Standards

- Indentacao: 2 espacos
- Bash: `set -euo pipefail` em todo script
- Funcoes: `snake_case`
- Variaveis: `snake_case`
- `source deployer/lib/*.sh` no inicio de cada ferramenta
- Conventional commits: `feat(deployer): ...`
- Log everything, never fail silently
- Hints obrigatorios antes de coleta de inputs
- Estado salvo apos deploy com sucesso

[Source: docs/architecture/coding-standards.md]

### Testing

- **Framework:** bats-core (Bash Automated Testing System)
- **Localizacao:** `tests/deployer/` com pattern `*.bats`
- **Helpers:** bats-support, bats-assert (ja instalados como devDependencies)
- **Cobertura minima:** 50%
- **Pattern de teste:** sourcear funcoes com `sed 's/^readonly //g'` para contornar readonly
- **Mocks:** Para testes de `stack_editavel()` e `wait_stack()`, mock de `docker` e `curl` commands

[Source: docs/architecture/tech-stack.md#testing]

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-02-20 | 0.1 | Story draft criado por @sm (River) | @sm |
| 2026-02-20 | 0.2 | Validacao GO (8/10 → 9/10 apos fixes) — Status Draft → Ready. Fixes: chmod 600 credenciais, clarificacao modularizacao vs standalone setup.sh | @po |
| 2026-02-20 | 1.0 | Implementacao completa — 57 testes passando (42 novos + 15 regressao) — Status Ready → InReview | @devops |

## Dev Agent Record

### Agent Model Used

Claude Opus 4.6 (claude-opus-4-6) — @devops (Gage)

### Debug Log References

Nenhum debug log necessario. Implementacao direta sem erros de runtime.

### Completion Notes List

- **Pre-Flight decisions:** Copiar/adaptar (lib/ independente), heredoc inline (Orion pattern), deploy sequencial, menu minimo
- Tasks 1-9: Implementadas em deployer/lib/*.sh + deployer/ferramentas/01-base.sh
- Task 10: deployer.sh com menu minimo funcional (opcao 01 ativa, demais EM BREVE)
- Task 11: 42 testes novos + 15 regressao = 57 total, todos passando
- **Decisao Pre-Flight:** Sem arquivos .tmpl — YAML gerado via heredoc inline em 01-base.sh (Orion pattern)
- setup.sh mantido intacto (standalone, zero dependencia em lib/)
- chmod 600 em dados_portainer conforme fix PO
- wait_stack com timeout 5min (10 iter x 30s) e associative array para multi-service
- stack_editavel implementada para deploys futuros via Portainer API (retry 6x)

### File List

- `deployer/lib/ui.sh` — step_ok(), step_fail(), step_skip(), step_init(), tabela(), resumo_final() (CRIADO)
- `deployer/lib/logger.sh` — log_init(), log(), log_finish() (CRIADO)
- `deployer/lib/common.sh` — dados(), recursos(), verificar_stack(), validar_senha(), conferindo_as_info() (CRIADO)
- `deployer/lib/deploy.sh` — stack_editavel(), wait_stack(), pull() (CRIADO)
- `deployer/lib/hints.sh` — hint_firewall(), hint_dns(), hint_provider() (CRIADO)
- `deployer/ferramentas/01-base.sh` — Ferramenta 01: Traefik + Portainer + Docker Swarm (CRIADO)
- `deployer/deployer.sh` — Menu principal do deployer (CRIADO)
- `tests/deployer/lib-ui.bats` — 10 testes (CRIADO)
- `tests/deployer/lib-common.bats` — 10 testes (CRIADO)
- `tests/deployer/lib-hints.bats` — 9 testes (CRIADO)
- `tests/deployer/lib-deploy.bats` — 5 testes (CRIADO)
- `tests/deployer/01-base.bats` — 8 testes de integracao (CRIADO)

## QA Results

### Review Date: 2026-02-20

### Reviewed By: Quinn (Test Architect)

**7 Quality Checks:**

| # | Check | Result |
|---|-------|--------|
| 1 | Code Review | PASS — clean bash, SetupOrion patterns, snake_case, set -euo pipefail |
| 2 | Unit Tests | PASS — 57/57 tests passing (42 new + 15 regression) |
| 3 | Acceptance Criteria | PASS — all 13 ACs verified |
| 4 | No Regressions | PASS — 15/15 setup.bats (Story 1.1) intact |
| 5 | Performance | PASS — timeouts configured, retry patterns with appropriate sleep |
| 6 | Security | PASS — chmod 600 credentials, password masked, read -s, set -euo pipefail |
| 7 | Documentation | PASS — story complete, Dev Agent Record filled, File List complete |

**Issues Found:** 2 low-severity (hint_dns default domain, portainer agent service naming)

### Gate Status

Gate: PASS → docs/qa/gates/1.2-ferramenta-traefik-portainer-docker-swarm.yml
