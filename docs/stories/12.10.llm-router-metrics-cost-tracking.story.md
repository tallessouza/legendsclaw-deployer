---
epic: 12
story: 12.10
title: "LLM Router Metrics, Cost Tracking e Skill Mapping Completo"
status: InProgress
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: [code_review, pattern_validation]
complexity: S
risk: LOW
---

# 12.10 — LLM Router Metrics, Cost Tracking e Skill Mapping Completo

## Executor Assignment

```yaml
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: [code_review, pattern_validation]
```

## Story

**As a** operador do agente,
**I want** que o LLM Router tenha métricas, cost tracking e skill mapping completo para todas as categorias,
**so that** eu possa monitorar custos e o router selecione o tier correto para cada skill.

## Description

### Contexto Arquitetural

O LLM Router é **nosso componente** (porta 55119), NÃO do OpenClaw. O OpenClaw apenas o vê como custom provider via `models.providers.llm-router` no openclaw.json.

```
OpenClaw Gateway
  → Chama models.providers.llm-router (http://localhost:55119)
  → NOSSO LLM Router decide tier via skill_mapping + keywords
  → Router chama modelo real (DeepSeek, Sonnet, Opus via OpenRouter)
  → Resposta volta pro gateway
```

O `skill_mapping` determina qual tier (budget/standard/quality/premium) usar baseado no contexto. Atualmente, o `generate_skill_mapping()` no 07-llm-router.sh faz pattern matching apenas nas 6 skills interativas. Com a Story 12.5 adicionando ~20 skills novas em 6 categorias, o mapping precisa ser expandido.

### Mudanças

1. **Métricas:** Adicionar `metrics.enabled`, `cost_tracking`, limites por tier
2. **Skill Mapping:** Expandir `generate_skill_mapping()` para cobrir todas as categorias
3. **Tier por Categoria:** Ler o `tier` do SKILL.md (campo no frontmatter) ao invés de pattern match no nome

## Acceptance Criteria

- [ ] AC1: Adiciona `metrics.enabled: true` e `metrics.cost_tracking: true` no llm-router-config.yaml
- [ ] AC2: Configura limites de custo por tier (budget: $0.01, standard: $0.10, quality: $2.00, premium: $10.00)
- [ ] AC3: Adiciona campo `analytics_endpoint: true` na seção metrics do llm-router-config.yaml (config-only, sem modificar JS)
- [ ] AC4: `generate_skill_mapping()` cobre todas as 6 categorias de skills (não só as 6 interativas)
- [ ] AC5: **Tier determinado pelo campo `tier:` no SKILL.md frontmatter** (budget, standard, quality, premium)
- [ ] AC6: Configuração existente do LLM Router permanece funcional

## Scope

### IN
- Modificar `deployer/ferramentas/07-llm-router.sh` (seção metrics + skill_mapping expandido)
- Expandir `generate_skill_mapping()` para 6 categorias
- Ler tier do SKILL.md frontmatter

### OUT
- Não implementa dashboard de métricas (apenas habilita coleta)
- Não modifica llm-router.js ou llm-router-server.js (apenas config)
- Não modifica openclaw.json (o router é custom provider, já registrado pela Story 12.0)

## Risks

| Risk | Impact | Mitigation |
|------|--------|------------|
| SKILL.md sem campo tier | Low | Default para "standard" se ausente |

## Tasks / Subtasks

- [x] **Task 1: Expandir metrics no 07-llm-router.sh**
  - [x] 1.1 Expandir seção `metrics:` no YAML gerado
  - [x] 1.2 Adicionar `cost_tracking: true`
  - [x] 1.3 Adicionar limites por tier
  - [x] 1.4 Adicionar campo `analytics_endpoint: true`

- [x] **Task 2: Expandir generate_skill_mapping()**
  - [x] 2.1 Escanear `apps/{agent}/skills/` recursivamente por SKILL.md
  - [x] 2.2 Ler campo `tier:` do frontmatter YAML de cada SKILL.md
  - [x] 2.3 Gerar mapping: skill_name → tier
  - [x] 2.4 Fallback: se tier ausente no SKILL.md, usar "standard"
  - [x] 2.5 Manter mappings default como fallback para skills sem SKILL.md

- [x] **Task 3: Testes**
  - [x] 3.1 Verificar YAML válido após modificação
  - [x] 3.2 Verificar que seção metrics tem campos novos
  - [x] 3.3 Verificar que skill_mapping cobre todas as categorias
  - [x] 3.4 Verificar fallback para skills sem tier no SKILL.md

## Dev Notes

### Seção Metrics Expandida

```yaml
metrics:
  enabled: true
  cost_tracking: true
  storage: none
  table: llm_metrics
  batch_size: 10
  flush_interval_ms: 30000
  retention_days: 30
  cost_limits:
    budget: 0.01
    standard: 0.10
    quality: 2.00
    premium: 10.00
  analytics_endpoint: true
```

### Tier por Categoria (derivado do SKILL.md)

| Categoria | Skills | Tier típico |
|-----------|--------|-------------|
| dev | react-best-practices, composition-patterns, etc. | standard |
| infrastructure | clickup-ops, n8n-trigger, etc. | standard |
| memory | context-recovery, todo-tracker, etc. | budget |
| orchestration | planner, task-orchestrator | standard |
| superpowers | brainstorming, writing-plans, TDD, etc. | quality |
| system | model-router, cost-monitor | budget |

### Skill Mapping Expandido (gerado)

```yaml
skill_mapping:
  # Infrastructure (existentes)
  clickup-ops: standard
  n8n-trigger: standard
  supabase-query: standard
  allos-status: budget
  alerts: budget
  budget-control: budget
  # Memory
  context-recovery: budget
  knowledge-graph: standard
  unified-memory: standard
  todo-tracker: budget
  # Orchestration
  planner: standard
  task-orchestrator: standard
  # Superpowers
  brainstorming: quality
  writing-plans: quality
  test-driven-development: quality
  executing-plans: quality
  verification-before-completion: quality
  requesting-code-review: quality
  dispatching-parallel-agents: standard
  systematic-debugging: quality
  # System
  model-router: budget
  cost-monitor: budget
  # Dev
  react-best-practices: standard
  composition-patterns: standard
  web-design-guidelines: standard
  vercel-deploy: standard
```

**Nota:** Este mapping é conceito do NOSSO LLM Router. O OpenClaw não sabe que existe — ele apenas chama `http://localhost:55119/v1/chat/completions` como custom provider. O router decide internamente qual modelo usar baseado no skill + keywords da mensagem.

## Testing

- **Framework:** BATS (Bash Automated Testing System)
- **Localização:** `tests/deployer/07-llm-router.bats`
- **Execução:** `npx bats tests/deployer/07-llm-router.bats`

### Cenários de Teste

1. **YAML válido** — Verificar que o config gerado é YAML válido após as modificações
2. **Campos metrics expandidos** — Confirmar presença de `cost_tracking`, `cost_limits`, `analytics_endpoint`
3. **Skill mapping completo** — Verificar que todas as 6 categorias (dev, infrastructure, memory, orchestration, superpowers, system) estão representadas
4. **Tier via SKILL.md** — Criar SKILL.md mock com `tier: quality`, verificar que `generate_skill_mapping()` usa esse tier
5. **Fallback sem tier** — Criar SKILL.md mock sem campo `tier:`, verificar que default "standard" é usado
6. **Fallback sem SKILL.md** — Verificar que skills sem SKILL.md usam o mapping default existente
7. **Regressão** — Verificar que config existente (models, tiers, keywords, fallback) permanece intacto

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-02-25 | 1.0 | Story criada a partir do Epic 12 | @pm (Morgan) |
| 2026-02-25 | 2.0 | v3 — expandido skill_mapping para 6 categorias, tier via SKILL.md frontmatter, clarificado que LLM Router é nosso componente (não OpenClaw) | @pm (Morgan) |
| 2026-02-25 | 2.1 | Validação PO: GO (10/10) — Status Draft → Ready | @po (Pax) |
| 2026-02-25 | 2.2 | Correções PO: AC3 clarificado (config-only), seção Testing adicionada | @po (Pax) |
| 2026-02-25 | 3.0 | Implementação completa: metrics expandidos, skill mapping via SKILL.md, 16 testes novos | @dev (Dex) |

## Dev Agent Record

### Agent Model Used
Claude Opus 4.6

### Debug Log References
N/A

### Completion Notes List
- Task 1: Seção metrics expandida com cost_tracking, cost_limits (4 tiers), analytics_endpoint. Default de métricas mudado de "n" para "s".
- Task 2: `generate_skill_mapping()` reescrita com 3 níveis de fallback: (1) SKILL.md discovery via find + frontmatter parsing, (2) dados_skills pattern matching legado, (3) default 14 mappings. Tier validado contra budget/standard/quality/premium, default "standard".
- Task 3: 16 testes BATS adicionados. Todos passam. 2 falhas pré-existentes não relacionadas (#42 anthropic_direct_fallback interpolação, #86 step_init count desatualizado).

### File List
| Action | File |
|--------|------|
| Modified | `deployer/ferramentas/07-llm-router.sh` |
| Modified | `tests/deployer/07-llm-router.bats` |

## QA Results

### Review Date: 2026-02-25

### Reviewed By: Quinn (Test Architect)

**7 Quality Checks:** All passed. 6 ACs verified. 118/120 tests pass (2 pre-existing failures unrelated to this story). No regressions detected. Security OK. Performance OK.

**Issue Found:**
- MNT-001 (medium): `((found_skills++))` may fail with `set -e` on first increment. Recommend `found_skills=$((found_skills + 1))`.

### Gate Status

Gate: CONCERNS -> docs/qa/gates/12.10-llm-router-metrics-cost-tracking.yml
